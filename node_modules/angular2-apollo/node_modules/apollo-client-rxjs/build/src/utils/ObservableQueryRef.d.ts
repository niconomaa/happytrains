import { ObservableQuery } from 'apollo-client';
export declare class ObservableQueryRef {
    private ref;
    constructor(ref?: ObservableQuery);
    setRef(ref: ObservableQuery): void;
    getRef(): ObservableQuery;
}
